function get_hour_from_num(num) {
    // Implement this
    if(num<12){
        return (num + " AM");
       } 
       else{
           return (num + " PM");
       }
    // return "this still needs some work";
}